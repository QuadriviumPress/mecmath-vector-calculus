<hr />
<footer>
  <em>{{ site.title }}</em> by
  <a href="{{ site.sourceUrl }}" target="_blank" rel="noopener">{{ site.author }}</a>
  (<a href="{{ site.libreTextsUrl }}" target="_blank" rel="noopener">LibreTexts</a>), licensed under the
  <a href="{{ site.licenseUrl }}" rel="license" target="_blank">{{ site.licenseLabel }}</a>.
</footer>

<script>
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register(window.Book.baseHref + '/sw.js');
    });
  }
</script>
